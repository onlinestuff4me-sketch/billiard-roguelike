node_modules/
dist/
dist-ssr/
*.local
.vite/
.DS_Store
Thumbs.db
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.idea/
.vscode/*
!.vscode/extensions.json

# Single-file build output (npm run build:single)
dist-single

# The classic-mode snapshot IS committed (public/classic) — Vercel builds one
# project from one repo, and a page it cannot build is a page it cannot serve.
# Regenerate it with `node scripts/snapshot-classic.mjs`, never by hand.
